server.port=8090

monitoring.health.app.enabled=false
monitoring.metrics.app.enabled=false

#kafka.bootstrap-servers=
#kafka.group-id=lamp-watcher

monitoring.health.kafka.enabled=false
#collection.health.kafka.bootstrap-servers=${kafka.bootstrap-servers}
#collection.health.kafka.group-id=${kafka.group-id}
#collection.health.kafka.topics=app.health

monitoring.metrics.kafka.enabled=false
#collection.metrics.kafka.bootstrap-servers=${kafka.bootstrap-servers}
#collection.metrics.kafka.group-id=${kafka.group-id}
#collection.metrics.kafka.topics=app.metrics


#export.kairosdb.url=

#export.health.kafka.enabled=false
#export.health.kafka.bootstrap-servers=
#export.health.kafka.topic=app.health
#export.metrics.kafka.enabled=false
#export.metrics.kafka.bootstrap-servers=
#export.metrics.kafka.topic=app.metrics


# Async ThreadPool
lamp.wathcer.async-thread-pool-core-pool-size=10
lamp.wathcer.async-thread-pool-max-pool-size=20

# Spring JPA
spring.jpa.properties.jadira.usertype.autoRegisterUserTypes=true

# Logging
logging.level.=INFO
logging.level.lamp.wacher=INFO
logging.file=./logs/lamp-watcher.log

# Endpoints
endpoints.health.sensitive=true
endpoints.shutdown.enabled=false
